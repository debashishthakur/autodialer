#!/usr/bin/env ruby
# This file was generated by Bundler, and is available as `bin/rails`
# when you run `bundle exec bin/rails`, just as it would be if you
# ran this file directly (i.e. `ruby bin/rails`).

require "rubygems"
require "bundler/setup" if File.exist?(File.expand_path("../Gemfile.lock", __dir__))

load Gem.bin_path("railties", "rails")
